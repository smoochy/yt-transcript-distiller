{
  "name": "yt-transcript-distiller",
  "version": "1.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0",
    "jest-fetch-mock": "^3.0.3",
    "jest-webextension-mock": "^4.1.1",
    "web-ext": "^10.4.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFiles": [
      "jest-webextension-mock",
      "./jest.setup.js"
    ],
    "setupFilesAfterEnv": [
      "jest-fetch-mock/setupJest"
    ],
    "transform": {}
  }
}
